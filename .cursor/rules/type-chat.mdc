# Cursor向けルール（type-chat）

詳細なコマンド・構成・規約・禁止事項は `CLAUDE.md` を参照する。ここでは重複して書かない。

## 必ず守ること（CLAUDE.mdの要約。詳細はCLAUDE.md側を正とする）
- 変更前に `CLAUDE.md` を読む
- 実装を変更したら、同じ変更でテストも直す
- `npm run check` が通ることを確認してから報告する
- `main` へ直接pushしない。PRをマージしない
- `.env` や鍵ファイルを読まない・作らない
